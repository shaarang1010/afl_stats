const initState = {
    teamGames: []
}

const gamesReducer = (state = initState, action) =>{
    switch (action.type) {
        case 'ADD_GAMES':
            
            break;
    
        default:
            break;
    }
}